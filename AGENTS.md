# AGENTS.md — 接手须知（任何 AI 工具先读本文件 · SSOT）

> **接手只读本文件**的「当前状态 + 下一步 TODO」即可继续。
> - **要改代码？先看 `docs/IMPL_NOTES.md` 顶部「锚点地图」**(函数/数据→行号)，据此 `Read offset/limit` 跳读那 20~40 行——**严禁宽读/宽 grep 那个 818KB 单文件**(整读≈42万 token，是接手耗 token 的唯一大头；grep 务必 `… | cut -c1-100`)。
> - 实现细节按需读 `docs/IMPL_NOTES.md` 对应段；完整变更历史看 `git log --oneline`；产品/设计 `docs/PROJECT_PLAN.md`/`docs/DESIGN.md`（仅里程碑/确需时读）。
> - **省 token（详见 `CLAUDE.md`「接手省 token 协议」）**：不预读 PROJECT_PLAN/DESIGN、不全量扫码、不 dump 大 diff（用 `--stat`/窄 grep）、只读需要的行段。
> - 完成改动：**日常只更新本文件**「当前状态/TODO」；里程碑才更 PROJECT_PLAN/DESIGN。状态写仓库文件（跨账号可读），不依赖工具私有记忆。

## 一句话
中国诗词知识图谱，面向小学生的**单文件离线 HTML** `poemgraph.html`。水墨基调（**禁红规则已取消**，配色可用朱砂红等暖色；视觉用 `/frontend-design` 迭代中）。

## 参考文档（按需读）
- `docs/DeviceLimit-Design.md` · 账号设备限制三方案设计（纯前端 UUID / Cloudflare Worker / CloudBase）；2026-07-11 选 A 方案但暂未实施，留作未来防盗版商升级时的备查

## 部署平台（2026-07-10 确认）
- **Render Static Site**：`https://pg-ndxn.onrender.com/poemgraph.html`
- **阿里 CNAME 自定义域**：`poem.leewen.work` → `pg-ndxn.onrender.com`（Cloudflare 反代）
- **账号管理工具**：`account-admin.html` 跟主站在同一 Render Static Site 部署
- **账号后端**（2026-07-16 迁移到 Neon Postgres）：`server/index.mjs` + `server/{app,auth,store}.mjs` + `server/schema.sql` —— Render Web Service `poemgraph-auth-api` 跑 `npm start`，数据落在 Neon `ep-fragrant-base...` 的 `poemgraph.accounts` / `poemgraph.devices` 表（与 WhiteBoard 共享同一 Neon project，`poemgraph.` schema 前缀隔离）。环境变量 `DATABASE_URL` / `AUTH_SECRET` / `ADMIN_TOKEN` 必须配，否则启动立刻抛错。
- **Render Dashboard Headers 配置（按需）**：Dashboard 左侧「Redirects/Headers」→ Add Header → Path `/accounts.json` / Name `Access-Control-Allow-Origin` / Value `*`。**当前未配**，但 admin HTML 已用「同源优先」策略绕过，详见下方当前状态条目。
- **仓库根 `_headers` 文件**：对 Render 无效（Render 不读）。留着不删是给将来迁回 Cloudflare Pages 时的兜底。

## 运行 & 测试
- 预览：`python3 -m http.server 8123` → `http://localhost:8123/poemgraph.html`
- **里程碑测试（强制）**：断网可用、五入口+详情正常、控制台 0 报错、iPad/桌面响应式。

## 硬约束（不可违反）
- 自包含 / 完全离线 / 无 CDN（当前 ~12.9MB，含内嵌音频三轨：普通话诗93+粤语诗93+历史故事9，每首诗开头先报「标题。朝代·作者。」）。**体积目标 ≤ ~15MB**，但真红线＝**目标 iPad 上秒开 + 滚动点击不卡**（体积≠流畅度：卡顿来自运行时动画/DOM，非字节数；十几 MB 主要是音频/文字时现代 iPad 浏览顺滑，仅开页多 ~1s）。**文字/SVG/数据几乎免费、随便用**；**内嵌音频/栅格图才是体积大头，需压缩克制**。若将来要"每首真人朗诵"(几十 MB)再改为"文件夹：HTML+音频"，仍离线。
  - **交付物已从"单一 HTML"演进为「`poemgraph.html`(自包含) + `sw.js`(可选缓存增强层) + `version.json`(在线版本哨兵)」**：`sw.js` 是同源 Service Worker，仅 http(s) 托管时由页面注册，把整份 HTML 持久缓存到本地→重访秒开+完全离线+可加主屏；`version.json` 仅在线时做小体积版本检查，避免手机 WebView 长期吃旧缓存。**`file://` 直接打开时 SW/版本哨兵不启用，`poemgraph.html` 仍是能离线跑的自包含单文件、行为不变**。故"离线/无外部依赖"初衷未破，只是字面"单文件"变三文件。内联音频拆成第二个 `<script>` 块(见下"手机卡顿"条)，两块脚本+SW 均已验证。
- 宣纸白 `#f7f5f0` / 浓墨 `#1c1c1e` / 墨青 `#2d5a6b` + **朱砂红 `#c1352b` 可作强调/印章/朱批**（禁红规则已取消，配色可演进；视觉用 `/frontend-design` 迭代中）
- 内容对齐**统编版**课本；进度/偏好存 `localStorage`（键 `pg_v1`），无账号系统

## 当前状态（2026-07-20）
- **2026-07-20：早发白帝城详情页加"夜郎（今贵州北部、遵义附近）"注释 + 用户自修 settings.json（双侧同步）**：用户原话"在早发白帝城这首诗的详情页面，在夜郎旁边添加一个注释：大致位于今天贵州北部、遵义附近"。两 HTML 各 3 处同步插入（早发白帝城·诗人故事 line 2014 / 历史时间轴 line 2016 / 李白长廊第四章）。anno 数组"夜郎=在今贵州一带"保留不动避免与详情页注释重复；sw.js 现为 v7（linter/你侧把 sw.js 升到 v7 后我先前的 v6 已无效，以 v7 为准），双 HTML 共享同一 SW 会被一起清缓存。**同日处理 `~/.claude/settings.json` JSON 报错**：line 36 缺逗号 + line 37 `"Write"` 键位置错误（应是数组元素而非 object key）。auto-mode 不允许我 Write / Bash 写该文件，最终用户在终端跑 `awk` 一段 + `python3 heredoc` 二段二修，JSON 合法（`json.load` 验证通过）；备份保留 `~/.claude/settings.json.bak.20260720_185414`（原错）和 `...20260720_233723_almost`（半成品）。  
- **2026-07-20：诗词地图补全李白足迹古今地名并避让《早发白帝城》标签（普通版 + Pro 同步）**：紫色李白一生足迹的 1–6 号点此前只有编号，没有地点名称。现补为“成都府（今四川成都）／白帝城（今重庆奉节）／庐山（今江西九江）／泾县（今安徽宣城泾县）／扬州（今江苏扬州）／长安（今陕西西安）”，并随“古地名／现代名”控件切换主次行；聚焦《早发白帝城》时隐藏与专属航线重复的 2 号白帝城足迹标签，起点白帝城与终点江陵改为向两侧上方错开的引线标签，避免古今名称压在圆点和路线之上。版本哨兵升为 `2026-07-20-map-route-labels`，SW 缓存升为 v7。
- **2026-07-20：《早发白帝城》地图纠正“荆门”重复观感并强化江陵终点（普通版 + Pro 同步）**：截图中的两个“荆门”一处是地图原标签、一处是点击同一地点后出现的浮动地点卡片；同时《渡荆门送别》曾把诗中的荆门山误落到今天的湖北荆门市。现按课文注释纠正为“荆门山（今湖北宜都）”并移动坐标；聚焦《早发白帝城》时隐藏无关诗作地点标签，只保留朱砂色长江路线及“① 白帝城（今重庆奉节）→② 江陵（今湖北荆州）”，且把单诗路线层提到普通地点标签之上，确保江陵不再被遮挡。版本哨兵升为 `2026-07-20-baidicheng-map-fix`。
- **2026-07-20：诗词地图历史事件由 9 条扩充至 23 条（普通版 + Pro 同步）**：围绕“确实改变现有诗人命运或创作主题”的标准新增 14 个节点，补齐汉魏、南北朝、中晚唐、宋元之际、明代与晚清空档：黄巾起义·汉末大乱、北魏统一北方、徐敬业起兵·骆宾王作檄、李白奉诏入长安、永王事件·李白获罪、永贞革新失败、牛李党争加剧、乌台诗案、绍兴和议、崖山海战·南宋灭亡、土木堡之变·北京保卫战、虎门销烟·经世思潮、秋瑾东渡日本、皖浙起义·秋瑾就义。每条均含儿童可读的事件说明、对诗人的具体影响及相关诗作入口，共建立 59 条事件—诗作链接；版本哨兵升为 `2026-07-20-poet-history-events`。
- **2026-07-20：《早发白帝城》地图新增单诗长江旅程（普通版 + Pro 同步）**：此前点击诗名只定位作诗地点“白帝城”，可见的紫色虚线是李白一生的概略迁徙足迹，并不包含诗句里的江陵。现新增独立的朱砂色诗作路线：点击《早发白帝城》后自动放大白帝城—三峡—江陵一段，标出起点“白帝城（今重庆奉节）”、终点“江陵（今湖北荆州）”，并沿长江标注“顺流而下约一千二百里 · 一日还”；紫色诗人足迹与朱砂单诗路线继续分层，避免混淆。版本哨兵升为 `2026-07-20-baidicheng-journey`。
- **2026-07-20：学生角色的详情解释改为共同学习口吻（普通版 + Pro 同步）**：详情页新增 `roleNarrative()`，仅在 `ST.role === 'student'` 时把面向学习者的第三人称措辞转成“我们”视角，例如《早发白帝城》延伸中的“孩子由此不只知道……”显示和朗读为“我们由此不只知道……”；同时把学生看到的“教师赏析”改为“我们一起想”。转换只匹配“作为孩子的启蒙课 / 适合孩子诵读 / 深受孩子喜爱 / 孩子由此”等讲述语，不会误改《村居》《池上》等诗中真实作为人物出现的孩子；家长、教师角色仍保留原文与相应标签。版本哨兵升为 `2026-07-20-student-narrative`。
- **2026-07-20：手机端顶部“年级”入口新增 1–9 年级下拉菜单（普通版 + Pro 同步）**：原入口只调用 `showView('grade')`，在已经处于年级诗词列表页时视觉上没有反馈。现于 820px 及以下点击“📖 年级”会展开 3×3 触控菜单，当前年级高亮；选择后更新全局年级、关闭菜单并停留在年级列表，点击外部或按 Escape 也可关闭。桌面端仍保持原有的年级页导航行为；Pro 仅额外显示 2–9 年级锁定标记并复用原付费墙拦截，其他交互与普通版一致。手机 390×844 浏览器回归：普通版展开/切换到三年级成功，Pro 展开及锁定标记成功，两页控制台 0 error；两页可执行内联脚本语法通过。版本哨兵升为 `2026-07-20-mobile-grade-menu`。
- **2026-07-20：详情页手机端新增应用内返回悬浮按钮（普通版 + Pro 同步）**：在 680px 以下的诗词详情页右上角显示 46px 米纸底圆形左向箭头，考虑 `safe-area-inset-top`，并提供 `aria-label`；点击直接复用 `backFromDetail()` 返回刚才的诗词列表/闯关页，不调用浏览器系统返回，避免微信等内置浏览器直接退出网页。
- **2026-07-20：Pro 未登录付费横幅桌面端改为单行文案**：「未登录用户只能使用 1 年级诗词」与「¥19.9 解锁全部内容 · 教师免费」改为左右并排且基线对齐，宽度不足时仍可自然换行；手机端「提示语 + 解锁」布局不受影响。
- **2026-07-20：Pro 未登录付费横幅手机端精简**：删除重复的「未登录限 1 年级 · 点击展开详情」摘要和 CSS 追加说明；手机/微信折叠态现只显示「未登录用户只能朗读 1 年级诗词」与一个「解锁」按钮，点击直接调用系统共用 `showPaywall()` 付费框；桌面端完整购买信息保持不变。
- **2026-07-20：手机微信端“诗词解释朗读条”改为点击后才显示（普通版 + Pro 同步）**：根因是 `fillDetail()` 在浏览器缺少 `speechSynthesis` 时主动调用 `panelTtsUnsupported()`，导致微信浏览器一进入详情就弹出底部 `panel-bar`，而 Firefox 因暴露该接口没有触发。现删除初始化阶段的自动提示与小喇叭禁用：进入详情/切诗时由 `panelClose()` 保持隐藏；只有用户点击“字词注释/诗中意象/炼字赏析/练习”等解释区小喇叭后，才开始朗读或显示“不支持语音朗读”。同时清理 `9f26c67` 遗留在 Pro `openDetail()` 内的孤立长诗按钮实验代码（该残留造成 `Unexpected token 'else'`）。两页各 199 个内联脚本语法通过，按需显示源码断言通过。
- **2026-07-20：Pro 详情页段落小喇叭恢复朗读与操作栏状态同步**：根因是全局 `panelBarShow()` 调用了 Pro 守卫 IIFE 内部的私有函数 `shouldShowTtsUnsupported()`，点击“字词注释”等段落的小喇叭后抛 `ReferenceError`，流程在 `setPanelBarBtn(true)` 前中断。现改为在 `panelBarShow()` 内直接判断 `speechSynthesis` 与移动端宽度，并同步启停朗读条控制按钮；本地真实点击验证操作栏会显示 `⏹ 停止`、`1 / 3`，两个页面各 199 个内联脚本语法检查通过。
- **2026-07-20：《早发白帝城》补入三年级上册并全链路接入（普通版 + Pro 同步）**：依据用户提供的统编版三年级上册 PDF 实页核对，该诗位于“语文园地六·日积月累”（PDF 第 91 页 / 教材页码 86）。新增 `baidicheng` 完整数据：原文拼音、9 条字词注释、白话译文/深度赏析、情感风格、5 个意象、759 年历史线、白帝城（今重庆奉节）地图坐标、传播度、炼字“轻/过”、4 首相关诗词和 3 类练习；`望天门山`、`峨眉山月歌` 同步加入反向相关。诗人故事按用户给出的共情材料重写为两层：正文讲“流放夜郎→途中遇赦→白帝城东下”，延伸串起“船轻、心也轻”“万重山也像人生困难”“低谷后重新获得希望”。新增语舒普通话 + 善怡粤语两条 22k 单声道离线 MP3（各约 30KB），并加入惰性音频块/ID 索引；按页面同款自适应 RMS 静音算法校验，两轨均得到 4 个严格递增的真实句间边界（普通话 2.54/4.38/6.19/7.97s，粤语 2.22/4.24/6.20/8.09s）。版本哨兵已升为 `2026-07-20-baidicheng`。最小检查：两页可执行脚本语法 OK、内容/关系/地图/炼字/音频断言全部通过。
- **2026-07-18：角色选择卡片换成三张彩铅人物图（普通版 + Pro 同步）**：`poemgraph.html` 与 `poemgraph-pro.html` 的学生/家长/教师卡片，已分别替换为用户提供的“小书童 / 亲子共读 / 女教师”图片。为保持两页离线自包含，原始 1254×1254 PNG（单张约 1.9–2.1MB）压缩为 256×256 JPEG（单张 17–19KB）后以内联 data URI 使用；卡片显示为 96×96 圆形头像，并加暖金细边与柔和阴影。桌面 1280×720、手机 390×844 浏览器回归通过：三图均 naturalWidth=256、显示 96×96、手机无横向/纵向溢出、控制台 0 error。
- **2026-07-17：admin.html 合并 5 hash 工具 + doLogin 双层校验（local → Neon fallback）**：用户反馈 Neo UI admin 缺了老 5-tab 工具（自动生成/批量/单条/验证/提取），要求保留 Neo 风格同时把 5 个 hash 工具 + paywall.json 配置块拼回去；另外要求保留 accounts.json 静态登录能力。**关键设计澄清（doLogin 语义）**：用户原话"优先从本地校验,本地校验不通过,再去 neon 校验,neon 校验通过的话,也通过"——这表示双层**不是串联权威**（"本地有→只看本地,本地没有→只看 Neon"），也不是**串联组合**（"本地对+ Neon 对才行"），而是**fallthrough 分流**：
- **2026-07-17：设备上限策略 = 信任 accounts.json 内的账号（修法 4）**：用户确认场景为「开发期单用户 + 上线后付费客户（数量小，1–20 范围内）」（见 `[[poemgraph-target-audience]]` + `[[poemgraph-no-users-yet]]`）。**取舍：accounts.json 内的账号不强制设备上限**。原因：pro.html:1456 的 doLogin 是 fallthrough 分流——**本地 SHA-256 校验通过即 return,根本不再调 Neon**,所以 Neon 的 `account.devices[]` 设备绑定表对 accounts.json 内的账号是"观察"而非"拦截"**。已知后果**:客户在 5 台机器登录,本地都能过(因为账号在 accounts.json),Neon 完全不知道——**设备上限被本地快路绕过**。**用户接受这个权衡**(理由:客户量小 + 真要封就改 admin 改 enabled=false)。**应用**:
  - 只在 Neon、不在 accounts.json 的账号(临时 admin 创建但未同步到 JSON)→ 设备上限**严格生效**(N 台拒)
  - accounts.json 内的账号 → 设备上限**仅观察**,不强制
  - admin 真要强制某个账号 → 在 admin HTML 表格的"启用"列 toggle off,该账号立刻在所有路径都拒
  - 已知改进方向(用户没催,先记):**改成「本地通过也异步调 Neon 记设备」**(修法 1),能记录但不阻塞登录,适合"超 3 台给警告,不阻拦"的场景。

  - 第一关本地 accounts.json + SHA-256：
    - u 找到 + 密码对 → **直接登录,跳过 Neon**
    - u 找到 + 密码错 → 拒(本地权威,密码错就是错,不 fall through)
    - u 找不到 → 标记 `_NOT_FOUND`,fall through 到 Neon
  - 第二关 Neon(`tryRemote`)：只对第一关说"我没这个账号"的请求才走
    - LOGIN_FAILED → 拒("用户名或密码错误")
    - DEVICE_LIMIT → 拒
    - 5xx / 不可用 → **拒**(不能让远端挂了就能随便进 — 本地没记录的账号必须由 Neon 确认)
    - 成功 → 登录,设 `mode:'remote'` token
  - **改动**：(1) `account-admin.html` 在 Neo UI 上插入"🛠 哈希工具"区块(5 个 tab:auto/batch/single/verify/parse,复用 df8e3ba 时期代码 + 适配 Neo 颜色 + 用 `adminSha256`),paywall.json 块保留底部;(2) `poemgraph-pro.html` `doLoginLocal` 找不到 u 时返回 `{u, _NOT_FOUND:true}`(不再 throw),`doLogin` 据此决定走 `tryRemote` 还是直接登录。新增 `tryRemote(u, p, deviceId)` 函数封装 Neon 调用。
  - **已知保留约束**：admin HTML 的「创建账号」按钮仍会创建只在 Neon 里的账号(不会自动写回 accounts.json);用户下次访问 admin 创建账号后,需手动跑一次"🔗 追加到现有 JSON → ⬇ 下载 accounts.json → git push" 同步。**部署**:push 后 Render 自动 redeploy,`accounts.json` 文件本身不改。
- **2026-07-14：生产前端默认指向 Render Auth API**：用户已部署 `https://poemgraph-auth-api.onrender.com`，`/health` 返回 `{"ok":true,"service":"poemgraph-auth"}`，并用 `https://poem.leewen.work/poemgraph-pro.html?api=https://poemgraph-auth-api.onrender.com` 登录成功；新浏览器再次用 `admin` 登录被拒，提示“此账号已在 1 台设备登录”，这是 `MAX_DEVICES=1` 的预期效果。已把 `poemgraph-pro.html` 和 `account-admin.html` 的 `<meta name="x-auth-api">` 写死为 `https://poemgraph-auth-api.onrender.com`，后续直接访问 `https://poem.leewen.work/poemgraph-pro.html` / `account-admin.html` 即默认走新 API，仍可用 `?api=` 临时覆盖。验证：`account-admin.html` 1 个 script 语法 OK、`poemgraph-pro.html` 197 个 script 语法 OK。
- **2026-07-13：Render Web Service 设备数量限制版本起步完成**：用户确认当前 Render 页面是 Static Site，无法运行 `/api/login`；新方案是在同仓库新建 Render Web Service 跑 Node 后端。**Render Free 推荐 Static Site + API Web Service 分离**：`poem.leewen.work` 继续托管静态 HTML，API 用 Render 自带域名或 `api.poem.leewen.work`，前端通过 `?api=`/`x-auth-api` 指过去，避免页面首屏被免费 Web Service 冷启动拖慢；若未来付费不休眠或想简化同源配置，再把 `poem.leewen.work` 迁到 Web Service（同一自定义域名不能同时绑两个 Render 服务）。新增 `render-auth-server.mjs`（无第三方依赖，Node 内置 HTTP server）：从 `accounts.json` seed 账号，`POST /api/login` 校验密码并按 `MAX_DEVICES` 绑定/拒绝设备，`POST /api/session` 校验 token+设备仍绑定，admin 接口支持 list/reset/add-device/set-max-devices；新增 `package.json`、`render.yaml`、`scripts/test_render_auth.mjs`、`docs/RenderDeviceLimit.md`，`.gitignore` 忽略本地 `data/`。`poemgraph-pro.html` 新增 `x-auth-api`/`x-auth-required`，登录改走 `AUTH_API + /api/login`，默认不再静默降级纯前端账号（除非 `x-auth-required=false`），启动时会远程校验已保存 session，设备解绑后会要求重新登录；`account-admin.html` 设备管理 tab 支持 `x-auth-api`，可查看/解绑/手动加设备/设置单账号台数。验证：`account-admin.html` 1 个 script 语法 OK、`poemgraph-pro.html` 197 个 script 语法 OK、`npm run test:auth` 通过（首次绑定/同设备登录/第二设备拒绝/调高台数/解绑/session 校验）。**限制**：当前设备数据库默认写 Render 本地文件 `data/auth-db.json`，便宜可试跑，但重启/重部署可能丢；商业稳定版后续应换 Render Disk/Postgres/Redis 或 EdgeOne KV。
- **2026-07-13：付费墙文案 4 个 stale 同步 bug 全修（admin→pro）**：根因 `WECHAT_ID` 是 IIFE 顶层 var 字面量，从不重读 LS；`buildPayCopy` 闭包捕获 `PRICE`/`WECHAT_ID` 全局 var。**4 个症状**：(B1) 新打开的 modal 仍显旧微信号；(B2) `buildPayCopy` 隐式依赖闭包 var；(B3) 「📋 复制微信号」剪贴板复制到旧 wx；(B4) banner `updateBanner` 用 `WECHAT_ID` 字面量。**修复**：`poemgraph-pro.html` 新增 `_readWx()` 函数 (line 1248) 从 `pg_pro_wx` LS / meta 双源读取；`WECHAT_ID` 改为 `var = _readWx()`；`refreshPayCopy()` 头部加 `WECHAT_ID = _readWx();` + 内联 wx IIFE 替换为 `_readWx()`；`buildPayCopy()` 改为 `buildPayCopy(price, wx)` 纯函数（接收参数而非闭包读取 var，`void wx` 占位未来扩展）；两处调用改为 `buildPayCopy(PRICE, WECHAT_ID)`。**Playwright 双 tab 实测**：(Case 1 BC 实时同步) admin 改 21.9 + leewen_2026_test → pro tab 已开 modal 立即变 + 新开 modal 也变 + clipboard 捕获 `leewen_2026_test` + banner 同步；(Case 2 显式 refreshPayCopy) `window.PAY_COPY.subtitle` = `添加作者微信购买 21.9 元`；(Case 3 LS fallback) 删 `pg_pro_price/wx` 后 refresh → 全部 fallback meta 默认 19.9 / leewen2017；控制台 0 错误。**改动量**：`poemgraph-pro.html` 14+/7- 行；不动 admin/sw.js/version.json。
- **2026-07-20：sw.js bump v5→v6 + 早发白帝城详情页加"夜郎（今贵州北部、遵义附近）"注释**：bump 缓存版本让 SW 不再 serve 旧字节（用户报"改了看不到"——SW 缓存优先导致），两边 HTML 各 3 处同步插入（早发白帝城·诗人故事 / 历史时间轴 / 李白长廊第四章）。同日处理：用户报告 `~/.claude/settings.json` JSON 报错（line 36 缺逗号 + line 37 `"Write"` 键位置错误），auto-mode 拦我写，改为用户在终端 `! python3 heredoc` 修复。
- **2026-07-11：sw.js v5 accounts.json 走 stale-while-revalidate**：用户提出"全是大陆用户、撤销账号延迟 ≤2-3s 可接受、离线能用"的诉求后扩展 SW。改动：`sw.js` bump v4→v5 + 新增 `ACC_CACHE = pg-acc-cache-v1` + 新增 accounts.json 拦截 handler（cached 0ms 返回 + 后台 fetch 更新；无缓存走网络；离线返回旧缓存或空 503）。**不动 boot()**：外层 `cache:'no-store'` 与 SW 内部 SWR 兼容，0 风险。**Playwright 实测**：SW 注册后 `pg-acc-cache-v1` 缓存生效；首次 fetch 16ms（实际本地无 RTT），第二次 1ms（命中 SW 缓存）；手动把 SW 缓存替换为含 demo4 的旧版，page fetch 立即返回 demo4——确认"撤销账号 ≤2-3s 延迟"语义正确。**当前状态（CLAUDE.md 同步补）**：仓库两条主 HTML 共享根 `sw.js` + `version.json`，后续改 SW 时必须考虑双页面影响（CLAUDE.md 新增"双 HTML + 共享基础设施"硬约束段）。控制台 0 个新错误。**已知旧 bug**：sw.js 写死 `CORE = './poemgraph.html'` 导致 poemgraph-pro.html 的 SW 缓存**没生效**（不属于本次范围，AGENTS 顶部已记）。
- **2026-07-10：Render 部署真相揭露 + admin 同源优先策略**：用户发 5 张 Dashboard 截图揭露"部署在 Render Static Site `pg-ndxn.onrender.com`，`poem.leewen.work` 是阿里 CNAME"——之前一直误判成 Cloudflare Pages。**Render 的 Headers 配置 ≠ 仓库根 `_headers` 文件**：Render 只读 Dashboard 里配（左侧「Redirects/Headers」入口），所以前一个 commit `1f822ef` 加的 `_headers` 根本没被读，远端 `/accounts.json` 自然没有 ACAO 头。即便作者去 Dashboard 加了 ACAO，还有个更微妙的根因：admin HTML 现在 meta 写绝对 URL `https://poem.leewen.work/accounts.json`，如果从 `pg-ndxn.onrender.com/account-admin.html` 打开就跨源，需要 CORS 才能通；只有从 `poem.leewen.work/account-admin.html` 同源打开才免 CORS。**新策略「同源优先」**：`var ACCOUNT_URL = (function(){ try{var u=new URL(location.href).searchParams.get('accountUrl'); if(u)return u;}catch(_e){}; var meta=...; if(meta){ try{var m=new URL(meta, location.href); if(m.origin===location.origin)return m.href;}catch(_e){}; } return location.origin+'/accounts.json'; })();`——优先级 `?accountUrl=...` > 同源 meta > 同源 location.origin；meta 跟当前 origin 不同则**降级到同源**避免作者忘了 Dashboard 配。**Playwright 实测**：`http://127.0.0.1:8765/account-admin.html` → meta 写 `poem.leewen.work/accounts.json` 跨源 → 降级到 `http://127.0.0.1:8765/accounts.json` → ✓ 已自动加载 2 条 demo 账号，状态条绿色 + textarea 288 字节；「🔄 重新拉取」点击后状态条中间态"正在从 ... 拉取…"+ fetch 真发出；控制台无新 CORS 错误。**未做**：Render Dashboard 实际配置（由作者点，AGENTS.md 顶部加了指引段）；仓库根 `_headers` 留着不删（万一迁回 Pages 还能用，但 GitHub 上要加注释说明它对 Render 无效）。
- **2026-07-10：明文按钮 textarea 权威化 + 重新拉取绕过 file:// 早 return**：用户真机反馈手动在 textarea 加 3 行后点 ⬇ 下载 TXT 只得到 3 行（自动生成那部分）+ 「🔄 重新拉取」无反应。根因：①`#a-copy-plain`/`#a-download-plain`/`#a-sync-json` 三个按钮都从 `loadPlainLog()`（localStorage）读，LS 只在 `#a-gen` 写入；②`parsePlainTextarea()` 正则 `^(\S+)\s{2,}(\S+)` 只匹配两字段，单字段手加行（如 `hehsujian`）被悄悄丢；③`loadExistingJson()` 在 `file://` 时早 return，按钮点也没用。修复：`parsePlainTextarea()` 扩展三种行格式（自动生成带汉字 / 手加 2 空格 / 手加单字段 p=''）；三个按钮统一改用 `parsePlainTextarea()`，textarea 是单一权威源；`#a-sync-json` 把解析结果**全量 sync 到 LS**（已有同名 → 改密码；空密码 → 跳过并提示 N 条），输出 JSON 末尾加「已跳过 N 条」备注；`loadExistingJson(opts)` 加 manual 参数，「🔄 重新拉取」传 `{manual:true}` 让 file:// 也至少试一次 fetch（看到 CORS 比静默好）。**Playwright 6 场景实测通过**：复制 6 行 / 下载 6 行 / 同步 5 条（跳 1 单字段）/ 空 textarea 三 alert 各正确 / 同步后再复制仍 6 行（不依赖 LS）/ 追加按钮：现有 2 + textarea 6 → 8 条全 hash 64 位 / 「🔄 重新拉取」状态条中间态正确且 fetch 真发出（拦截 fetch 数 1）；控制台仅 3 个 CORS 报错（远端 ACAO 仍缺，见下面）+ blob mock 残留 1 个，**0 个真脚本错**。⚠️ **`_headers` 部署仍未生效**：远端 `curl -I https://poem.leewen.work/accounts.json` 显示 `cache-control: public, max-age=0, s-maxage=300` 是新的（说明 `_headers` 文件被 Pages 读到了），但**响应头里仍无 `Access-Control-Allow-Origin`**——怀疑被 Cloudflare 边缘层某种机制（domain 接入/CDN 反代）过滤掉了 CORS 头。已 commit `6a7d006` 但**远端 fetch 自动填 textarea 这件事仍未真打通**，剩 TODO。
- **2026-07-10：CORS 修复 + admin 自动拉取 + UX 文案**：上一账号接手解决 account-admin.html「🎲 生成账号」无响应（user22「多一个 `}`」是误诊，**实测按钮本身工作正常**：点 `#a-gen` 后明文 textarea + localStorage 都被正确写入 3 条；UX 问题仅在按钮只填明文不动 JSON 输出框，已留作 UX 提示）。本次**真正卡住的根因**是远端 `https://poem.leewen.work/accounts.json` 缺 `Access-Control-Allow-Origin` 响应头（`curl -I` 确认；Cloudflare Pages 静态托管默认不带 CORS）→ `loadExistingJson()` 即便代码写对了仍被浏览器跨源拦截。修复：①新建仓库根 `_headers`（Cloudflare Pages 原生支持），给 `/accounts.json` 加 `ACAO: *` + `s-maxage=300`（账号哈希本来就是 pro 版公开清单，索性开放跨源读，未来要做付费墙再换白名单/Worker）；②`account-admin.html` `loadExistingJson()` catch 分支加 CORS 失败一键诊断文案（提示去看 `_headers`）。**验证**：本地 `python3 -m http.server 8765` 起 HTTP、Playwright MCP 加载 `account-admin.html` → fetch 被 CORS 挡（预期，headers 未部署），状态条渲染新文案「⚠️ 自动加载失败：Failed to fetch（点击「📁 从本地文件加载」或手动粘贴）· 多半是远端 `accounts.json` 没配 CORS 响应头，详见仓库根 `_headers`」；用 `DataTransfer` 喂本地 `accounts.json` 文件 → 状态条「✓ 已从本地文件加载（2 个账号）」，`#a-existing` textarea 写入 288 字节。**未验证**：`_headers` 部署到 Cloudflare Pages 后远端 fetch 是否自动填 textarea——待用户真机打开 `https://poem.leewen.work/account-admin.html` 复测。
- **2026-07-10：account-admin.html 账号管理接手修复 + 自动加载 accounts.json 收尾**：用户从 Claude Code 中断处交接，截图控制台报 `Uncaught SyntaxError: Unexpected token '}' (account-admin.html:530)`，导致 `🎲 生成账号` 无响应。根因：`#a-gen` 点击函数已在约 503 行闭合，后面残留 `window.__pg_newAccounts = newOnes; };`，既多了一个 `}`，又引用了已出作用域的 `newOnes`。修复：把 `window.__pg_newAccounts = newOnes` 挪回生成函数内部并删除多余闭合；同时完成 Claude 原任务：复用 `<meta name="x-account-url">`（与 `poemgraph-pro.html` 同字段）自动拉取现有 `accounts.json` 填入 `#a-existing`；`file://` 或无 meta 时不自动 fetch、textarea 留空并提示手动粘贴/「📁 从本地文件加载」；fetch 失败也留空并提示本地文件兜底；保留 `?accountUrl=` 临时覆盖和「🔄 重新拉取」。验证：Node 解析唯一 `<script>` 通过；模拟 `file://` fetch 调用数 0 且 existing 为空；模拟 `http://` fetch 成功填入 demo 账号并显示 `✓ 已自动加载`；模拟无 meta 不 fetch；模拟 fetch fail 留空并显示本地文件/手动粘贴提示；模拟点击 `#a-gen` 可生成 3 行明文账号。**真实浏览器回归**：本地 `python3 -m http.server 8124`，打开 `http://localhost:8124/account-admin.html?accountUrl=http://localhost:8124/accounts.json`，自动加载 2 个现有账号；真实点击「🎲 生成账号」生成 3 行明文；真实点击「🔗 追加到现有 JSON」输出 5 个账号且所有 hash 64 位；真实点击「🔄 同步到 JSON（重算哈希）」输出 3 个明文账号、`version=1`、`salt=pg-pro-salt-v1`；打开 missing URL 时 existing 留空并提示「📁 从本地文件加载」/手动粘贴；以上浏览器控制台 0 error。`file://` 真浏览器导航被 Browser Use 安全策略拦截，已由 Node 模拟覆盖。
- **2026-07-09：详情页朗读·全 panel 扩展 · 5 场景 Playwright 回归已跑**：HTTP server `8123` + Playwright MCP，结果 **3/5 通过 + 2/5 受限但证据齐全**。**通过**：场景 2「诗意 tab 朗读启动」(panelSpeaking=true, queue=3, panelBar show, 按钮 ⏹ 停止)、场景 3「朗读中切 tab 立即停」(panelStop 守卫实测：连调 3 次仅 cancel 0 次，不误杀原文/事件 TTS)、场景 4「⏹ 按钮停止」(panelSpeaking=false, 按钮 ▶ 朗读, panelBar 保留)、场景 5「空闲态点 ⏮⏭ 不抛」(⏮/⏭/▶/setPanelRate 4 按钮空闲态全不抛、0 console error)。**受限**：场景 1「原文朗读不乱读」——Playwright headless Chromium 的 TTS 有 191 voices 但 speak() no-op（Chrome 已知），无法真测听感；但**控制台 0 个真错（仅 favicon×2 404）、TDZ 已修（无 TypeError）、var panel* 已挪到 printSheet() 后**——"乱读"能修好的前置条件全部具备。**剩余真机确认项**：用户复测听感是否真不乱读。TODO 第一项「真机回归」保留 `[ ]` 未划（仅剩真机听感验证）。
- **详情页朗读·全 panel 扩展（不识字小孩友好）**：详情页 8 个 panel 中"原文"tab 已有成熟朗读（语舒/美嘉/Li-Mu 双轨 audio+TTS，AGENTS 旧条目），**其他 7 个 panel（诗意/诗人/历史/地图/图谱/练习/相关）本次也加了朗读**。**架构决策**：不重新发明 PoemReader 抽象层，直接复用现有 `eventNarrate(:4766)` 的"段落数组→逐句 speak + cancel 兜底 + `_ttsKeep` 保活 + `pickNarratorVoice` 男声"模式，**独立命名空间** `panelSpeaking/panelQueue/panelIdx/panelVoice/panelRunId/panelRate/panelTabKey/panelTotal/panelPrevHi`（**已在 printSheet() 后、boot() 之前声明，TDZ 已修**）。**2 轮关键 bug**：①plan 误把"事件模态框 9 段音频"当成"详情页历史 panel 音频"——实际详情页历史 panel 必须纯 TTS；②`var panel*` 原本在 panelNarrate 函数定义附近（5097 附近），但 `boot()` 在 4890 调 `showView`→`panelTtsDone`→`panelStop`，TDZ 抛 `TypeError: Cannot read properties of undefined (reading 'length')`，导致年级页诗词列表消失/控制台断在 boot。**修法**：`var panel*` 整批挪到 printSheet() 后、boot() 前（约 4881 行）。**朗读条 UI**：详情页内新增 `#panel-bar`(`poemgraph.html` DOM 1064 附近，CSS 407 附近)：▶/⏹/⏮/⏭ + 进度点 dot×N + 慢/正常/快三档 + 关闭，与已有 `#d-recite-float`（原文朗读）并存不冲突。**每个 panel 数据层**在 `fillDetail`（约 2770 行）新增 `_panelSections_xxx` 数组 + 给 .sec 加 `data-read-section` 钩子 + 段背景朱砂红高亮；**练习 tab**额外注册 `_panelSections_quiz` 题计划 + 在 `pickChoice/pickMatch/submitQuiz` 钩短反馈（"答对啦！"/"还差一点，正确答案是 X"）和最终反馈段。**相关 tab** 推荐语动态合成（同一作者/同朝代/共享意象/题材相近，零数据改动）。**取消误杀修复**：`panelStop` 加守卫 `if(!panelSpeaking && !panelQueue.length)return;`，避免无条件 `speechSynthesis.cancel()` 误杀原文/事件 TTS。**lint-pass 后状态**：用户反馈"年级页回来了"（第二个截图），但**原文朗读是否还乱读、其他 panel 朗读是否稳**，尚待用户真机回归。**未实机验证项**：6 个非原文 panel 的 TTS 朗读、练习答题反馈 TTS、切 tab 立即停止、TTS 兜底降级（无 speechSynthesis 设备）。**改动量**：`poemgraph.html` 新增 ~350 行 panel 引擎+UI+钩子，重构 1 处（fillDetail）。
- 旧条目见下方"详情"——按惯例从上往下越新越在顶部，但下方完整条目仍保留以备查。
- **版权信息（作者 LiJiaWen）**：三处——① 文件顶部 `<!DOCTYPE html>` 后的 HTML 注释块（含"应用界面与代码版权归 LiJiaWen 所有；第三方内嵌素材如古琴曲另按其原授权协议不在此范围内"的措辞，避免跟已有的 CC BY-SA 素材冲突）；②`<head>`里`<meta name="author" content="LiJiaWen">`；③角色选择页(`#role-overlay`)底部新增`.copyright-line`可见小字"© 2026 LiJiaWen"(`--ink3`浅灰色，不抢视觉)。
- **详情页朗读操作栏分组修复**：`打印学习单`+`彩色/省墨` 原本是两个独立的 flex 子项，行宽不够换行时会各自换行、彼此分离(彩色/省墨飘到下一行开头，跟打印按钮脱节)。改用 `.d-print-group`(`poemgraph.html:385`附近)把两者包成一个整体 flex 项(左侧加细分隔线`border-left`)，换行时整组一起换，视觉上打印相关的控件始终连在一起。
- **详情页上一首/下一首导航 + 听诗入眠/磨耳朵播放方式切换**：详情页标题下方新增 `#d-nav`(`renderDetailNav(p,questMode)`，`poemgraph.html:2532`附近)，按 `visiblePoems()`(卡片墙/图谱/地图/时间轴共用的按年级累计列表)顺序显示"‹上一首/下一首›"(复用`.d-back`极简样式)，`gotoAdjacentPoem(dir)`跳转；列表两端对应按钮消失(不首尾循环)；**闯关模式(`ST.questMode`)下隐藏**(闯关有自己的按年级精确匹配+逐关解锁的列表和门禁，不能被这里的自由导航绕过)。听诗入眠/磨耳朵播放器新增"播放方式"胶囊(`#sleep-loop`，复用`.sleep-timer`样式)：`sleepLoopMode`(`pg_sleeploop`持久化)="seq"(默认，即改动前行为，按诗单顺序播放)或"single"(单曲循环，`sleepOnEnded()`里一首放完不推进`sleepIdx`直接原地重播)。已验证：导航正确跳转+边界隐藏+闯关隐藏+相关tab跳出闯关后按钮重新出现；单曲循环/顺序播放两种模式下`sleepIdx`推进行为分别正确(用不依赖真实音频的纯逻辑单元测试隔离验证，避免本环境背景音频播放对`ended`事件测试的干扰)；控制台0错误。
- **人物关系·朋友圈**（图谱新功能）：图谱工具栏「🤝 朋友圈」入口 `openCircle('libai')`。SVG 自环图(`#circle-modal`)：中心人物+一圈关系人，**同代＝实线**(墨青`#2d5a6b`)、**跨代影响＝虚线+箭头**(赭`#a0603a`，前人→后人，`inf:1` 时 `a`=源头)、**非诗人＝赭色节点+「非诗人」标注**。**每条边 hover/点 → tip 显「戴建业式俏皮话(`fun`)大字 + 依据(`desc`)小字」**(`showCircleTip(i)`，给小学生好记又可考查；`.ct-fun`/`.ct-basis` 样式；标签带 ⓘ 提示可点)，点头像换看 TA 的朋友圈，点中心看本人简介。数据：`FIGURES{}`(5 非诗人:汪伦/唐玄宗/严武/房琯/赵明诚)+`RELATIONS[]`(33 条经核对的真实关系，每条 `{a,b,type,desc,inf?}`)；另补 8 位无诗诗人(高适/元稹/孟郊/苏辙/黄庭坚/欧阳修/谢朓/柳宗元)入 `POETS` 仅供朋友圈引用。**新诗人/FIGURES 无 `POEMS` 条目 → 不出现在图谱/卡片墙/地图**(那些由 POEMS 构建)。函数：`openCircle/closeCircle/renderCircle/showCircleTip/personOf/personRelations/_ce`(紧接 `setGLayout` 后)。〔已测：33 关系端点全解析、边tip依据、人物跳转、箭头方向、控制台0错误。〕
- **朗读·三音色分工**：音色＝语舒/美嘉/Li-Mu(`recVoiceChoice` 存 `pg_recvoice`，**默认语舒**)。**语舒＝离线内嵌音频** `RECITE_AUDIO`(92首语舒 mp3 base64，**单字符串/首** `RECITE_AUDIO[id]`，**每首开头先报「标题。朝代·作者。」再读正文**，整首播，全设备可用——解决小米等国产安卓无 TTS 引擎不发声)；**美嘉/Li-Mu＝浏览器系统 TTS**(零字节，逐句高亮，浏览器不支持会 confirm 提示)。`reciteStart`：语舒有内嵌即播 `recitePlayAudio`，否则及美嘉/Li-Mu走 TTS；详情页朗读统一走 `reciteHighlightLine`，高亮句变化时自动滚到屏幕中间偏上，长诗/词后半段不再被底部遮住；按钮为**朗读/暂停/继续**三态(`recPaused`)，点击任意诗句(`recitePickLine`)可从该句起播/跳播，TTS 用 `recRunId` 过滤 cancel 后旧回调，内嵌音频用 `recAudioMeta` 按字数估算 seek，并通过 `REC_AUDIO_HI_ADVANCE=0.75s` 让高亮略提前贴近声音。浏览器无音色/看门狗1.6s无声→confirm 切语舒内嵌。**粤语朗读**(`recLang=cantonese`)：内嵌 `RECITE_AUDIO_YUE`(92首善怡 zh-HK，同样先报题)，手机也能播，故粤语按钮始终可用(`recApplyLangAvail` 不再因无浏览器粤语音色置灰)；`recitePlayAudio(p,track)` track='yue'选粤语轨。**历史故事朗读**(`openEvent`→`eventNarrate`)：优先内嵌 `EVENT_AUDIO`(9事件语舒，`eventPlayAudio`+`evtAudioEl`)，否则浏览器男声 TTS。生成 `scripts/gen_recite_audio.py`(**三轨**：语舒普通话诗+善怡粤语诗+语舒历史故事，AVSpeech `/tmp/synth_batch.swift`→ffmpeg 22k 单声道→base64，复用 `/tmp/rec3` 已有片段免重合成——**改报题/正文文本须先删对应 `.caf`/`.mp3` 缓存再跑**，否则按文件名命中旧缓存不重录；扩覆盖改 `MID_PICK` 再跑)。文件 ~12.2MB(在 ~15MB 软目标内——音频按需解码不伤流畅度,真红线是秒开+不卡;**勿降音质**)。
- **诗词听读·夜读电台**(共用一套播放引擎 `#sleep-player`，两个导航入口)：🌙**听诗入眠** `openSleep('sleep')`(默认安神诗单 `SLEEP_CALM` 已扩到42首，均为已有内嵌音频ID，几乎不增体积；默认30分定时，睡前场景)；🎧**磨耳朵** `openSleep('eartrain')`(默认按当前年级+不限时循环，给宝宝磨耳朵)。播放器内**诗单类型选择器** `#sleep-picker`(安神入眠/全部/各年级，`sleepBuildList`/`sleepGradesAvail`/`sleepSetPlaylist`；九年级已有7首内嵌音频，桌面选择器加宽可直接看到，手机保持横滑)；另有**底部起播诗单抽屉** `#sleep-drawer`/`#sleep-list-body`，按钮 `#sleep-list-btn`，`renderSleepList/sleepOpenList/sleepPickIndex` 可从当前 `sleepList` 任意一首开始播，并随切诗/切诗单同步高亮；旁边有**古琴开关** `#sleep-amb-btn`(`pg_sleep_amb`，默认开，关后切诗/继续播放也不自动开)。长诗布局为**顶部选择器固定可见 + 中间 `.sleep-stage` 独立纵向滚动(无横向滚动条/夜读色滚动条) + 底部控制固定可见**。连播内嵌音频(`sleepTrack` 按 `recLang` 选普/粤轨)、**整首逐行高亮并自动滚动到当前句**(`sleepUpdateUI` 渲染全诗为 `.sl-ln`,`ontimeupdate` 按字数估时逐句点亮;**报题段 `sleepPreW` 不点亮诗句**)、**暂停/继续同步停止/恢复低音量古琴垫底(仅古琴开关开启时)**、**诗间留白 0.8s**(原2.5s,改为与句间节奏一致)、**睡眠定时**(墙钟 `sleepStopAt`+~6s 渐弱 `sleepFadeStop` 抗后台限流)、**锁屏 MediaSession**、`onended` 链式连播(息屏续播)。〔测试勿真播：会在用户机器出声，验逻辑即可。〕
- **字词注释（进行中）**：详情页原文「就地注释」——有注释的词带朱批虚线下划线+①②③上标圈码，悬停(桌面)/点击(触屏)弹释义气泡，底部清单同号。数据复用 `anno:[{w,m,k?}]`(k=匹配键，用于非连续/消歧)。已注释 36 首(木兰诗+蒹葭+送杜少府+13+5+7+10)；渲染器对任何有 `anno` 的诗自动生效。剩余初中约 27 首待补(A 我录/B 用户贴课本)。
- **133 首**（小学70+初中63，含《木兰诗》《关雎》等，年级1-9全覆盖）；批量扩充流水线（pypinyin+多音字FIX，新诗只需正文+元数据+短译文，`autoQuiz` 自动测验）
- 六入口（年级/卡片墙/知识图谱/地图/时间轴/诗人长廊）+闯关；三角色（学生/家长/教师）+探索/学习模式；详情页8区块；三类练习+得星；5维搜索；教师打印
- **知识图谱**：力导向+伪3D；单击信息卡 / **双击展开1-hop（累积式·再双击折叠·封顶12+重置）**；聚焦总览(朝代+诗人)；诗人节点圆形+全名、按产出名气定大小+多产金边；三布局(force/radial/tree)
- **诗词地图**：缩放平移、2D浮雕山脉、标注4选1(古/今/名胜/无)、名胜图标、诗人足迹、按诗人展开、墨珠地标+悬停浮标、历史大事点「讲」→事件卡+男声朗读
- **时空联动(D)**：底部年份游标播放→逐年点亮地图+诗作列表(E 两栏/筛选/定位)
- **氛围(F)**：详情逐句朗读+普通话/粤语切换(语舒/善怡)；古琴背景(程序合成/内嵌《阳关三叠》CC BY-SA 署名)；诗人剪影头像(性别×朝代冠服)
- **诗人长廊(C)**：选择器+面板(称号头条/传奇人生章节故事/代表作/足迹)。**「传奇人生」叙事**(`POET_STORY{epithet称号,tagline矛盾,chapters[{h,body,quote,from}],coda升华}`，`renderPoetPanel` 有则显故事+章节竖轴+朱批名句+升华卡、无则回退 `po.intro`)：已写 **28 位**大家(7首批+唐一线/宋名家/唐名家/魏晋明清四组共21位)，其余次要诗人/无名合集(诗经/汉乐府等)保留简介待定。**故事内古地名一律附今地名**(如「碎叶城（今吉尔吉斯斯坦境内）」)，**所嵌名句须用库内真实诗作且出处准确**。样式 `.poet-epithet/.poet-tagline/.story-ch/.story-quote/.story-coda`。
- **家长画报·迁徙金线图**(A4竖版可打印)：5诗人迁徙路线，每人**一色一线型**(`TRACK_STYLE`：色+虚实双重区分，灰度打印也能分清)，`trackStyle(k)` 取样式。**已从装饰海报改造为「亲子陪读任务单」**：尾部加「✏️边走边问」引导问题+留白(`buildMigrationPoster`)，引导问题数据 `POET_JOURNEY_Q{poet:{q,why}}`(5诗人,真实史实,屏幕地图+打印共用单一来源)。
- **打印学习单·三角色版**(`buildPrintSheet(p,role)`，**打印按钮在详情页常驻操作栏 `#d-print`**[朗读那行]，所有标签页可见、标签随角色由 `fillDetail` 设；原藏在测验页已移出)：**学生**=自测单(原文+注释+自测题+留白)；**家长**=完整离屏学习单(让孩子在纸上学,减少看屏；+白话译文+练习+「家长陪读提问」复用 `POET_JOURNEY_Q`)；**教师**=班级作业单(姓名/班级栏)。`printSheet()` 传 `ST.role`。**彩色/省墨双版**(`printColor` 存 `pg_printcolor`，操作栏 `#print-mode` 切换)：彩色=`colorSheet`(莫兰迪调色板 `PRINT_PAL` **按 `p.emo` 选色** `printPalette`、楷体居中诗、**金句淡金高亮**`printGoldLines`(取自 `POET_STORY` 引用句)、标签式注释、页脚远山飞鸟 SVG、`print-color-adjust:exact`)；省墨=`plainSheet`(黑白朴素,整班打印)。
- **交互地图·边走边问**：地图选中诗人(`mapPoet`)时 `renderMapList` 头部显 `POET_JOURNEY_Q[mapPoet].q` 提示框(`.ml-ask`,朱批左边)，所有角色可见(课堂/亲子/自学通用)
- **视频录屏展示页**：根目录 `video-showcase.html` 已创建（7页场景化录屏页：开场/三类用户/学生/图谱探索/家长/教师/镜头清单；中文主讲+少量英文副标题；N 显示/隐藏旁白，←→翻页，按钮切到真实 `poemgraph.html`）。
- **手机端首访角色/年级点击保护（方案1，未拆音频）**：`poemgraph.html` 在角色选择前加极小早期脚本，主 JS/大音频脚本未完成时先接住 `pickRole/pickGrade` 点击，主脚本 `flushEarlyPicks()` 回放；**用户反馈猪猪加载层让体感更慢，已撤销可见加载动画与350ms保底等待**；搜索 `initSearch()` 改为首次打开搜索面板才初始化；`sw.js` bump 到 `poemgraph-cache-v2` 且 install 阶段不再强制预缓存 12.9MB HTML，避免手机首访后台重复下载。**仍保留单文件离线版；若手机仍慢再做方案3(Render 极速入口)。**
- **朗读句间停顿/高亮(当前状态 v15，v1-v14的字数比例估算调参演进历史见 `git log`，不赘述)**：详情页 `#rec-speed` 三档**跟读(follow,0.65+句间真停顿2.5s供模仿)/慢读(slow,0.65连续)/正常(normal,0.75，默认)**；听诗入眠 `#sleep-speed` 三档**舒缓(0.55)/慢读(0.65)/正常(0.75，默认)**，三档都有句间停顿0.5s(不紧不慢)。**v1-v14 反复调"按字数比例猜每句时间边界"的各种常数(高亮提前量/暂停余量/报题段权重/标点停顿加成……)，每次都能改善当次反馈但换首诗又出偏差——根本原因是内嵌音频没有真实逐字时间戳，纯猜的模型有精度天花板**。**v15 用 Web Audio API 解出真实音频波形，测出人朗读时的真实静音间隙，取代猜时间**：新增 `analyzeReciteGaps(dataUri,expectedN)`(`poemgraph.html`，紧邻 `AC`/`ambInit`)——`AC.decodeAudioData` 解码(复用古琴功能已有的 AudioContext 单例)→算RMS音量包络(20ms窗/10ms步长)→用**各录音自己的噪声底/峰值百分位数**自适应定阈值(天然兼容普通话/粤语不同底噪，不用再分语言调)→找连续≥150ms的静音区间当停顿。**触发点用静音区间的"起点"而非中点**(`ontimeupdate`浏览器事件本身有~100-250ms检测滞后，用起点无论滞后多久都还在安全的静音范围内,用中点滞后可能已经越过静音进到下一句)。**间隙数量匹配**：多余的间隙只会出现在开头(报题段"标题。朝代·作者。"自己内部的标点停顿)或结尾(念完最后一句到文件结束的尾部静音)，真正的诗句分界必然连续分布在中段——所以过滤掉贴近`duration`的尾部间隙，若还多就从最前面丢，只留最后 `expectedN` 个；数量对不上(比expectedN还少)就整体返回`null`，不猜测具体缺哪个。`getReciteGaps(poemId,track,dataUri,n)` 按 `poemId+'|'+track` 缓存 Promise，同一首诗同一音轨只解码分析一次，详情页跟听诗入眠共用缓存。**接入方式是纯增量**：`recitePlayAudio()`/`sleepPlayCurrent()` 里旧的字数比例模型(`_pre`/`_w`/`_tot`及各种调了几轮的常数)一行没删，继续无条件计算着作为回退；新起一个`_recRealGaps`/`sleepRealGaps`(null=分析中或失败→走回退分支)，`ontimeupdate`按"有真实间隙就用它算`idx`(且不再需要武装延迟等待,`armMs`直接为0)，没有就走原字数比例分支"两条路径；懒加载(第一次真正播放某首诗才触发分析，那一次播放退回估算模型，之后有缓存直接用真实值)。**已验证**：普通话/粤语的静夜思、小池(逗号句号交替)、敕勒歌(3~7字长短句混杂，此前v14修完仍有偏差的诗)全部一次性检测到与诗句数完全匹配的真实间隙，缓存去重、非法数据优雅回退null(不抛异常)、真实点击/`reciteStart()`全流程零报错均已用真实函数验证；受限于工具环境的后台标签页限制，`decodeAudioData`本身不受影响(它是一次性计算不依赖`<audio>`连续播放)，但完整的"听感是否真的更准"仍需用户实机确认——**若真实检测对某首诗数量不匹配而回退，那首诗的听感就还是旧的估算模型水准(不会比之前差，只是没有变得更准)**。
- **闯关引导修复（⚠️待用户点击验证，浏览器自动化本轮不可用）**：用户反馈闯关模式点开诗后不知道要做练习题才能解锁下一关。修复：`openDetail` 检测 `ST.view==='path'` 时设 `ST.questMode`，在「练习」tab 加脉冲红点(`.quest-target`+`questPulse`动画)+ 详情页顶部显示 `.ml-ask` 风格提示条(`#quest-banner`,"🚩 闯关中·完成「练习」解锁下一关")；`submitQuiz()` 通关后追加「返回闯关」按钮(`showView('path')`)；顺手修复 `backFromDetail()` 原本闯关模式下会退回卡片墙而非闯关页的 bug。**仅做了 JS 语法校验+逐函数调用链人工追踪，未做真实浏览器点击验证**——已提交但标记待确认。
- **闯关按年级修复（✅已由用户点击验证：7年级正确从"峨眉山月歌"起步）**：`renderPath` 原用 `visiblePoems()`(累计`grade<=ST.grade`，卡片墙/图谱等其他视图故意用累计，不能改)。修复：**只改 `renderPath` 内部**，改用 `POEMS.filter(p=>!ST.grade||p.grade===ST.grade)`(精确匹配所选年级)，不影响其他视图。各年级诗词量9~23首，够单独闯关。
- **顶栏年级下拉改为原地切换（⚠️待验证）**：用户反馈点顶栏年级chip会跳去年级选择大蒙层，选完又固定跳到"年级"视图，如果原本在闯关/图谱等页面会被强制带走。修复：`#grade-chip` 由 `<button onclick="openGradePick()">` 改为原生 `<select onchange="quickPickGrade(this.value)">`(9个选项)，新函数 `quickPickGrade(g)` 只更新 `ST.grade`+`updateChips()`+`showView(ST.view)`(重渲染当前视图，不跳走)。**保留** `pickGrade()`/`openGradePick()`/大蒙层不变，仍用于首次未选年级时的引导流程。`updateChips()` 里原来 `grade-chip.textContent=...` 相应改成 `.value=...`(select 用 value 不用 textContent)。**2026-07-03 视觉重设计**：用户反馈原生下拉箭头太贴圆角边框、且不清楚它与顶栏 tab 的关系。已确认 `#grade-chip` 是驱动所有视图 `visiblePoems()` 的**全局年级筛选器**(跨全部 tab)，**保留**。新增 `select.chip#grade-chip` 专属 CSS(CSS 顶部 `.chip` 规则后)：`appearance:none` 去原生箭头 + 内嵌 SVG 自绘水墨小三角(`background-position:right 12px center`)+ `padding-right:30px` 留呼吸空间；`body.theme-zhumo` 下箭头改朱砂 `#c1352b`。**已用 Claude in Chrome 真实浏览器验证**：本地 HTTP server 起 `poemgraph.html`，`getComputedStyle` 确认 `appearance:none`+30px右内边距+SVG背景生效；箭头在水墨/朱墨两主题下均正确留白且配色随主题切换；`quickPickGrade('5')` 回归测试确认切年级后停留在原视图(诗词地图)且诗数正确变化(34→61)、控制台0错误。✅
- **诗人长廊·查询体验**：①详情页「诗人」tab 新增「📜 查看XX完整传奇人生 →」跳转按钮（仅 `POET_STORY` 有数据的诗人显示），点击 `poetSel=key;showView('poets')` 跳到诗人长廊对应诗人，避免与传奇人生长文重复维护。②诗人长廊头部新增搜索框(`poetFilterQ`,按姓名子串)+朝代快捷筛选chip(`poetFilterDyn`,复用`.sf`样式,`selectPoetDyn`)，无匹配显示空状态提示；函数 `renderPoetPicker/renderPoetDynFilters`。已用 puppeteer-core+本机 Chrome headless 测试：搜索/朝代过滤/空态/跳转链接均正确，控制台0错误。✅
- **诗人长廊·按模式联动年级筛选**：用户发现顶栏年级选择器不影响诗人长廊列表(选1年级仍显示曹操/陶渊明等初中诗人)，跟卡片墙/图谱/地图/时间轴/闯关(均用 `visiblePoems()` 累计过滤)不一致。讨论后采用**探索/学习模式区分**方案(而非一律过滤或一律不过滤)：`poetKeysSorted()`(`poemgraph.html:3707`) 改为 `ST.mode==='learn'` 时用 `visiblePoems()` 过滤(累计到所选年级，与其他 tab 一致)，`explore` 模式不过滤(诗人图鉴自由浏览，保留探索乐趣)。已用 Claude in Chrome 真实浏览器验证：探索模式下69位诗人固定不受年级影响；学习模式1年级→10位(骆宾王/李白/孟浩然/白居易/贾岛/杨万里/唐寅/汉乐府/佚名等，均为真实拥有一年级教材诗作的诗人)，9年级→69位(与探索模式总数一致)；`poetSel` 在过滤后自动回退到列表首位(如原选中曹操被过滤掉，自动切到骆宾王且详情面板正确更新)，控制台0错误。✅
- **长诗朗读按钮悬浮分身（✅已修复，用户2026-07-05真机点击确认通过）**：用户反馈长诗(如62行《木兰诗》)滚到底才找不到「朗读」按钮。新增 `#d-recite-float` 悬浮分身(`.d-recite` CSS块后+`#v-detail` `</section>`后)，详情页里主按钮 `#d-recite` 不可见时才显示；`setReciteBtn(on)` 同步主/悬浮两按钮文案与`.on`态。**连续修了四个坑，前三个都不够**：①"是否在详情页"误写 `ST.view==='detail'`(`showView` 进详情页时故意不更新 `ST.view` 以便返回，条件永远false)→改判 `#v-detail.classList.contains('on')`。②离开详情页只调 `reciteStop()` 没摘 `.show`，按钮卡在年级页等页面→`showView` 的 `v!=='detail'` 分支(`poemgraph.html:2411`)新增直接 `remove('show')`。③`new IntersectionObserver(...).observe()` 没存引用被GC回收→存到模块级变量。④**真正的根本问题**：`IntersectionObserver` 只在"可见比例跨越阈值"时才回调——短诗从"不存在"变"存在且可见"是 0→>0 的真实跨越，会触发；但长诗从"不存在"变"存在但已在视口外"，比例从头到尾是 0，从未跨越阈值，**回调根本不会触发**，这才是长诗从来没显示过的真凶(前三个坑修的判断逻辑本身没错，只是压根等不到回调)。**最终方案**：弃用 `IntersectionObserver`，改为 `updateReciteFloatShow()`(直接 `getBoundingClientRect()` 判断，`poemgraph.html:4723` 附近) + `scroll` 事件(`rAF` 节流)持续更新 + `openDetail()` 里换诗那一刻主动调一次(`poemgraph.html:2498`，不必等首次滚动)。**验证方式教训(供以后测本地文件改动参考)**：本机测试服务器(`python3 -m http.server`)配合 Chrome 缓存，编辑文件后直接刷新可能仍加载旧版本，务必用 `?v=N` 递增参数强制绕过缓存，否则会把"测的是旧代码"误判成新代码的问题。**这次是真正的端到端验证**(不再是手动模拟条件)：真实调用 `openDetail('mulan')` 后 `#d-recite-float` 的 `.show` 直接为 `true`(不依赖任何后续回调)，`openDetail('yonge')`(短诗)为 `false`，`showView('grade')` 后为 `false`，重新打开木兰诗又为 `true`；配了截图佐证——木兰诗详情页右下角实际渲染出朱红悬浮按钮，切到年级页后截图确认消失；控制台0错误。用户此前经历三轮"这次应该真修好了"但仍失败，这次(根因是阈值跨越语义)在真机上点击确认通过，功能闭环完成。
- **古琴背景默认不再随朗读自动开、面板可关闭且窄屏不出界**：用户反馈①点朗读时古琴背景音会自动混进来干扰听诗 ②手机上古琴设置面板(`#amb-pop`)有时超出屏幕且没有关闭按钮。**古琴自动播放**：`reciteStart`/`recitePlayAudio` 原本各有一行 `if(!ambOn){ambAutoR=true;ambStart();...}` 在朗读开始时自动开古琴混音，已删除(连同 `reciteStop` 里对应的 `ambAutoR` 清理、及已全局无用的 `ambAutoR` 变量声明一并移除)；古琴现在完全手动，只能通过顶栏「古琴」按钮自己开，朗读不再自作主张插入背景音。**面板可关闭+防出界**：`#amb-pop` 新增右上角「✕」关闭按钮(`closeAmbPanel()`)；新增 `positionAmbPanel()`——视口宽度 `>560px`(平板/桌面)维持原有"绝对定位在按钮右侧"的样式；`≤560px`(手机)时改用 `position:fixed;left:12px;right:12px`+按顶栏实际 `getBoundingClientRect().bottom` 动态算 `top`(不硬编码顶栏换行后的高度)，配合 `max-height:calc(100vh-90px);overflow-y:auto` 防止内容在矮屏下被裁切；`resize` 时重新定位。已验证：桌面宽度下面板 `style.cssText` 为空(沿用原 CSS 定位，未受影响)，模拟窄视口(`Object.defineProperty(window,'innerWidth',390)`)下面板正确变为贴边 `fixed` 定位；点击✕关闭正常；朗读开始/停止全程 `ambOn` 保持 `false`；控制台0错误。**用户同时问到"是否要整体做手机版适配"——已建议单独立项**(项目硬约束目前只声明 iPad 768px+/桌面 1024px+，手机窄屏从未是目标)，本次只解决这两个具体反馈点，未做全局响应式改造。
- **朋友圈·唐宋八大家充实**：用户反馈"朋友圈"人物太少，建议加唐宋八大家(韩愈/柳宗元/欧阳修/苏洵/苏轼/苏辙/王安石/曾巩)。排查发现库里已有6位(韩愈`hanyu`/柳宗元`liuzongyuan`/欧阳修`ouyangxiu`/苏轼`sushi`/苏辙`sushe`/王安石`wanganshi`)，缺苏洵、曾巩；且多数人关系边极稀薄(欧阳修/苏辙/王安石/柳宗元此前各只有1条边，点开朋友圈很空)。**新增**：`POETS` 补 `suxun`(苏洵)、`zenggong`(曾巩)两个"仅供朋友圈引用"条目(无 `POEMS` 条目，不会出现在诗人长廊/卡片墙，同 `sushe`/`ouyangxiu` 已有模式)；`RELATIONS` 新增8条真实核实关系(韩柳并称·古文运动战友；苏洵与苏轼苏辙父子×2；欧阳修举荐苏洵/同科取仕苏轼苏辙/提携曾巩/赏识王安石×4；曾巩王安石同乡荐友×1)，把稀薄的边补齐到每人2~7条；顶栏「看谁的朋友圈」quick-jump 分组为"李白/杜甫/王维/白居易/曹操"+新增"唐宋八大家：韩愈/柳宗元/欧阳修/苏洵/苏轼/苏辙/王安石/曾巩"整组8人可一键跳转(此前只有韩愈、苏轼两人可从顶层直接跳)。已验证：8人 `personOf`/`personRelations` 全部正确解析，`openCircle` 逐一调用无报错，曾巩(此前最薄的新增项)截图确认正确显示欧阳修(跨代虚线)+王安石(同代实线)两条关系；控制台0错误。
- **手机端角色选择点击卡顿——根因定位并修复(单文件内 script 拆分，未破坏离线约束)**：用户反馈手机上点"学生/家长/教师"角色卡响应很慢。**根因**：`poemgraph.html` 全文件 12.9MB，其中 `RECITE_AUDIO`(~5.0MB)+`RECITE_AUDIO_YUE`(~5.2MB)+`EVENT_AUDIO`(~1.15MB)+`GUQIN_CLIP`(~0.3MB) 四个内嵌音频常量合计 **~11.6MB，占全文件 90%+**，此前全部挤在同一个 `<script>` 标签内——浏览器对经典(非 async/defer)`<script>` 是整块同步解析执行，执行完之前排队的点击事件不会被派发；桌面机测得 `domInteractive`≈228ms 感觉不出，但手机 CPU 慢很多倍，用户"点了没反应"很可能是主线程还在同步解析这 11.6MB 音频数据字面量，点击事件排队等着。**修复**：把 4 个音频常量从原来穿插在函数定义中间的位置，整体挪到 script 末尾并单独用 `</script><script>` 切成第二个标签(纯用 `awk` 按行提取+重新拼接，逐字节校验无内容丢失)；核心 UI 逻辑(133首诗数据+全部函数)留在第一个、小得多的 script(≈0.46MB)里，先解析执行完即可响应点击，音频数据在后台的第二个 script 里继续加载，不阻塞交互。**踩坑记录**：第一版直接在音频常量原地(穿插在函数中间的位置)切 script 标签，导致 `boot()`(在音频数据之前调用)引用了定义在音频数据*之后*的 `reciteStop()`——不同 script 标签间函数不提升，直接抛 `ReferenceError: reciteStop is not defined`；**教训：拆分单文件里的巨型内联数据时，必须把数据整体挪到所有常规代码之后，而不是就地在数据当前位置切标签**，否则会把原本靠"同一脚本内提升"跨越数据块前后调用的函数关系切断。**验证**：拆分后2个 script 块各自语法校验通过；返回用户(已选角色+年级)`boot()`路径控制台0错误(此前踩坑版本这里会报错)；全新用户 `pickRole`→年级弹层正常；`RECITE_AUDIO`/`RECITE_AUDIO_YUE`/`EVENT_AUDIO`/`GUQIN_CLIP` 均可正常访问，实际调用 `reciteStart()` 确认走 audio 模式播放；卡片墙/图谱/地图/时间轴/诗人/闯关/年级 七视图 `showView` 全部无报错；桌面本地测得 `domInteractive` 228ms→115ms(减半，手机上因 CPU 慢差距会远大于此)。⚠️**未做真机测试**——本轮只在桌面 Chrome + 本地服务器验证正确性和相对耗时改善，手机上"点击后是否秒响应"仍需用户在真机(尤其是移动网络下)实测确认。
- **上条修复引出的真机回归：朗读误报"不支持"，已修复**：用户真机实测确认点击响应变快了(~2秒)，但随后反馈打开一首诗点朗读，弹出"当前浏览器不支持语音朗读，且这首诗暂无离线音频"——这首诗其实有内嵌音频。**根因**：脚本拆分后，用户完全可能在那 11.6MB 音频数据(第二个 script 标签)还没从网络下载完时就点开诗、点了朗读；此时 `RECITE_AUDIO` 还是 `undefined`，`reciteHasAudio(p)` 误判为"无离线音频"，叠加手机 Chrome 本身 `speechSynthesis` 常常 0 音色，两条路都不通，就报了这个误导性极强的错误。**修复**：新增 `_audioReady`(全局标记，第二个 script 末尾 `GUQIN_CLIP` 之后置 `true` 并 flush 回调队列)+`onAudioReady(cb)` 注册表(`poemgraph.html` 第4569行附近声明)，`reciteStart()` 开头先查 `_audioReady`，没就绪则按钮文案变"⏳ 音频加载中…"、注册就绪回调，直接 `return`，不再往下走到"判定不支持"那段逻辑；15秒兜底超时以防万一网络彻底卡死。**踩坑**：第一版就绪回调里直接自动重新调用 `reciteStart()` 播放，结果 `<audio>.play()` 在非用户手势的异步回调里被浏览器自动播放策略拦截，静默回退到停止状态(現象：`recMode` 变回 `null`)——**教训：任何触发 `.play()`/`speechSynthesis.speak()` 的重试，都必须发生在真实用户手势(click)里，不能在 setTimeout/Promise 等异步回调里"帮用户点一下"**。改成：数据就绪后只把按钮文案变回"🔊 朗读"，播放本身留给用户的下一次真实点击触发。**验证**：模拟"未就绪→点朗读→显示加载中→就绪→按钮回正常→用户再点一次→真正播放(`recMode:'audio'`)"全链路，且验证了数据已就绪的正常情况(多数场景)完全无感知延迟；控制台0错误。
- **上条 `_audioReady` 方案仍被真机否掉→重写为直接查数据本身，已修**：用户手机(WiFi)实测——看到"加载中"后再点朗读，仍弹"不支持语音朗读且无离线音频"(以《静夜思》为例，实测该诗确在 `RECITE_AUDIO`，92首之一，内容没问题)。**真正根因**：上一版用 `_audioReady` 布尔标记 + **15秒定时器兜底**，而手机下载+解析 11.6MB 第二个 script 常常远超十几秒；定时器先于数据到位就把 `_audioReady` 置 `true`(谎报就绪)，此时 `RECITE_AUDIO` 仍 `undefined`，`reciteStart` 放行后 `reciteHasAudio` 仍为假 → 误弹 alert。**教训：就绪判断不能用时间兜底，必须用「数据本身是否已定义」**。**重写**：删除 `_audioReady`/`onAudioReady`/15s 定时器/块尾 flush 那整套；新增 `reciteAudioReady()＝return typeof RECITE_AUDIO!=='undefined'`(数据真身)；`reciteStart` 未就绪时显示"加载中"并每 400ms `setTimeout` 轮询，就绪后把按钮恢复"🔊 朗读"交给用户再点(仍遵守"播放须在真实手势内"，不自动 play)。因本文件是单一自包含 HTML、第二个 script 是同文档一部分、下载必然完成，轮询保证收敛，无需也不该有时间兜底。**验证**(桌面 Chrome，用 `reciteAudioReady` 打桩强制返回 false + `getVoices` 返回空模拟手机无 TTS)：未就绪点朗读→显示"加载中"且**不再弹 alert**(旧版这里必弹)；轮询到就绪→按钮回"朗读"；再点→真正播放内嵌音频(`recMode:'audio'`)；数据已就绪的正常场景→一点即播、无"加载中"闪现；控制台0错误。
- **角色/年级选择加载动画·可爱猪猪**：用户反馈选完角色后有感知加载(手机上~2秒)，问能否加进度条。讨论后结论：**不做精确进度条**(没有可靠 API 知道内嵌音频具体加载到几成，装假百分比不诚实)，改做**纯 CSS 驱动、不需要 JS 逐帧刷新的可爱动画**(`#pig-loader`，`poemgraph.html` `grade-overlay` 前)——一只原创设计的圆脸小猪(非某知名卡通形象，避免版权问题)，`@keyframes` 实现整体弹跳+耳朵摆动+眨眼+尾巴摇摆，全部由 CSS/GPU 合成线程驱动，就算主线程正忙(如后台还在解析内嵌音频)动画也不会卡成静止画面。`pickRole`/`pickGrade` 包一层 `withPigLoader(work)`：先同步显示猪猪，`setTimeout 30ms` 让浏览器先画出动画再执行真正逻辑，且**保底显示≥350ms**避免设备快时一闪而过。已验证：猪猪+尾巴层级正确(尾巴放在body椭圆后绘制，不被遮挡)，完整"清空登录态→点学生→点一年级→落地年级页"全链路走通、猪猪按预期显隐，控制台0错误。
- **Service Worker 离线缓存(PWA)——手机重访免重下 11.6MB 音频**：用户问能否让音频大文件本地持久缓存、下次访问免加载。因音频全内嵌在单个 HTML 里，"缓存音频"＝"把整份 12.9MB HTML 存本地"。**当前策略**：同源 `sw.js`(仓库根，与 poemgraph.html 同级)，对 poemgraph.html 用**缓存秒开 + 后台校验 + 自动刷新**(`CACHE='poemgraph-cache-v6'`，**2026-07-20 bump v5→v6**：理由是用户在 v5 缓存上看到的页面仍缺"夜郎（今贵州北部、遵义附近）"新注释，bump 强制清掉旧缓存，下次首访重下 ≈12.9MB HTML；双 HTML 共享同一 SW，故 Pro 版同样被清)：有缓存时立即返回缓存(保留秒开)，后台用缓存里的 ETag/Last-Modified 向服务器条件请求，未变 304 极小，变了才下载新版、更新缓存并自动刷新当前 poemgraph 页面；没有校验头时后台比较 HTML 内容，只有真的变了才刷新，避免刷新循环；离线/网络失败则继续用缓存。**修复过的关键坑**：普通用户不会使用 `?v=commit` 复杂链接，旧的缓存优先会让微信/手机 WebView 长期显示旧 HTML；v4 升级时会删除旧 `poemgraph-cache-*` 并对已打开的 poemgraph 页面自动 `client.navigate()` 刷新一次。另新增小文件 `version.json` + 页面端 `PG_APP_VERSION/pgCheckVersion()`：在线打开后只请求几百字节版本哨兵，发现远端版本不同就注销旧 SW、删除 poemgraph 缓存、自动用内部 `?pgv=` 拉最新版，并用 `history.replaceState` 把地址栏恢复成普通 URL；这样普通用户仍只用原始链接。带 `?v=…` 查询串的请求仍一律放行走网络(开发期取最新代码不被缓存挡)。`poemgraph.html` `boot()` 后注册 SW：**仅 `location.protocol` 以 http 开头(即 http(s) 托管)时注册；`file://` 直接打开时 SW 不启用**，并用 `updateViaCache:'none'`+`registration.update()` 尽快检查新版 SW。交付物因此从"单一 HTML"变"HTML + sw.js + version.json 增强层"(见"硬约束"条更新)。**验证(localhost 是 SW 安全上下文)**：注册成功+active+controller 接管+core 已缓存；杀掉本地服务器后重新导航仍完整加载(133首诗+RECITE_AUDIO 全在、`navigator.serviceWorker.controller` 为真)；离线下 `reciteStart` 仍正常播内嵌音频(`recMode:'audio'`)；控制台0错误。
- **搜索解除年级门禁**：用户反馈顶栏选"一年级"时搜"木兰诗"(实为七年级课文)查无结果、且不知道原因——搜索和筛选是两种心智模型，被遗忘的筛选器悄悄拦住搜索比"真没有"更糟。讨论后选定方案：**搜索永远全量检索** `POEMS`（`doSearch` 不再先过 `visiblePoems()`），年级信息从"门禁"改为结果卡片上的**徽章**(`.sr-grade`，显示 `p.volume` 如"七年级下册"，样式用 `--silk`/`--silk-line` 暖米色小标签，紧跟标题后)。**其他视图(卡片墙/图谱/地图/时间轴/闯关/诗人长廊学习模式)的年级筛选完全不受影响**——只解除了搜索面板这一处。已用 Claude in Chrome 真实浏览器验证(`poemgraph.html:4357` `doSearch`)：一年级筛选下搜"木兰诗"→1命中+"七年级下册"徽章正确显示(此前会因 `grade<=1` 被过滤为0命中)；同年级内搜"咏鹅"仍正常+徽章"一年级上册"；按"作者"维度搜"李白"→13命中(5维筛选逻辑未受影响)；清空搜索词→133条(=`POEMS.length`全库总数，证明真正解除年级限制)；`visiblePoems()` 在一年级/九年级下分别仍是12/133(其他视图年级筛选逻辑分毫未动)；控制台0错误。✅
- **v15后续：手机端跟读仍会带出下一句半个字——已定位根因并加rAF高频轮询修复(⚠️待真机验证)**：用户反馈手机上跟读"咏鹅"，"曲项向天歌"读完后会带出下一句"白"字读音约0.3个字才停。排查：用`analyzeReciteGaps`实测该诗真实间隙`[2.95,3.49,5.38,6.94]`，数量与4句诗完全匹配(检测本身没问题)；但"曲项向天歌→白毛浮绿水"这段真实静音只有**280ms**(这首儿歌本身节奏轻快，停顿天生偏短)。**真正根因**：v15把跟读的"武装延迟"(`armMs`)在有真实间隙时设为0，完全依赖`ontimeupdate`事件"检测到已进入静音区"这一步——但手机浏览器的`ontimeupdate`轮询间隔常比桌面粗(实测/经验上可到250ms+甚至更久，尤其安卓/省电模式)，如果检测延迟超过这段静音本身的280ms，代码反应过来时已经冲进下一句。桌面之前测试一直"看起来调好了"是因为桌面`ontimeupdate`轮询本身更密集，没暴露出这条短间隙的风险。**修复**(`recitePlayAudio`/`sleepPlayCurrent`，`poemgraph.html`)：把原来`ontimeupdate`里内联的整段"判断念到第几句"逻辑抽成具名函数`reciteCheckProgress`/`sleepCheckProgress`，**`ontimeupdate`继续绑定它、行为不变**；额外新增一路`requestAnimationFrame`循环，在同一个播放周期(`recRunId`/`sleepRunId`过滤，播放结束/切歌自动停)里以显示器刷新率(~16ms/帧，远密于`ontimeupdate`的原生节流)反复调用**同一个**函数——谁先发现跨过真实间隙谁触发暂停，两路调用同一个幂等函数(靠`changed`/`prevLn`比较防重复触发)，纯增量，未改任何既有分支的判断逻辑本身，只是把"发现已进入静音区"这一步的检测延迟从大概率的几百毫秒压到一帧。rAF若因标签页在后台被浏览器整体节流(锁屏/切走)会退化到跟改动前一样(仅`ontimeupdate`那一路)，不会更差。**验证**：本地语法校验通过(3个`<script>`块均可解析)；用假状态模拟真实间隙跨越序列驱动`ontimeupdate`直连调用，`recHiIdx`按`[2.95,3.49,5.38,6.94]`四个间隙依次推进、暂停/续播计时链未报错——确认重构未改变原有分支行为。**受限于本环境浏览器自动化对隐藏标签页`<audio>`播放和`requestAnimationFrame`真实节奏的限制，"手机上这段280ms的短停顿是否真的不再被吃掉"这一核心问题仍需用户真机(尤其是之前复现问题的那台安卓手机)实测确认**——若真机确认修好，可标记此条完成；若仍复现，说明该设备的`requestAnimationFrame`本身也被节流到不够用，需要进一步升级方案(如Web Audio的`AudioContext.currentTime`高精度时钟或`MediaSession`级别的调度)。
- **上一首/下一首会跳年级——已修复**：用户反馈9年级下"下一首"从五年级《己亥杂诗》直接跳到九年级《临江仙》。根因：`POEMS`数组核心课标部分按年级升序，但末尾"日积月累"补充诗不严格单调递增(`jihai`grade5后紧跟着`linjiangxian`grade9)，`gotoAdjacentPoem`/`renderDetailNav`原直接用`visiblePoems()`的原始数组顺序，卡片墙同样用这个顺序但格局展示不明显，"下一首"强调连续性就很突兀。修复：新增`navOrderedPoems()`(`poemgraph.html:2553`附近)＝`visiblePoems().slice().sort((a,b)=>a.grade-b.grade)`(稳定排序，同年级内保留原相对顺序)，仅`gotoAdjacentPoem`/`renderDetailNav`这两处改用它，不改`visiblePoems()`本身(其他视图顺序不受影响)。已验证：己亥杂诗前后邻居变为同为5年级的《山居秋暝》/《四时田园杂兴·其三十一》；`gotoAdjacentPoem(1)`真实跳转正确；`navOrderedPoems().length`与`visiblePoems().length`一致(133，未丢诗)；控制台0错误。
- **手机首次打开慢~30秒——已定位根因并重构音频内嵌方式(⚠️待用户真机验证)**：用户反馈手机上打开(微信内置浏览器本地文件`content://`、及手机浏览器打开托管Render网址)都要等约30秒，且明确"两种场景都是第一次慢、第二次快"。这个规律排除网络/硬件天生慢的解释，指向**一次性JS解析/编译成本被浏览器引擎bytecode缓存命中**：四个内嵌音频常量(`RECITE_AUDIO`≈5.0MB/`RECITE_AUDIO_YUE`≈5.2MB/`EVENT_AUDIO`≈1.15MB/`GUQIN_CLIP`≈0.3MB)此前是**JS对象字面量**，浏览器必须把11.6MB当JS源码完整解析编译，不论用户点不点朗读；此前"拆两个`<script>`标签"的优化只解决了排序(音频解析推迟到UI逻辑之后)，没减少这个解析工作量本身。**修复**：把四个常量改成**惰性非执行的`<script type="text/plain" id="aud-{track}-{poemId}">`数据块**(194个，浏览器当DOM文本节点存着、完全不解析编译，真正播放时才用`document.getElementById(...).textContent`惰性取出+内存缓存)；新增极小的`RECITE_AUDIO_IDS`/`RECITE_AUDIO_YUE_IDS`/`EVENT_AUDIO_IDS`索引数组(只是"有没有这首诗音频"的静态事实，几KB，页面刚加载完就绪)。**就绪判断精确到"这一首诗、这一条音轨"**(`reciteAudioReady(p)`→`audioClipReady(track,id)`直接查`getElementById`，不查全局标记)——这是关键设计点：194个数据块是依文档顺序陆续解析进DOM的，用户追问"点朗读会不会变慢"后发现若用全局标记会把"文档靠后的诗还没解析到"误判成"已就绪"。**数据块按"普通话(cmn,92)→粤语(yue,92)→历史事件(evt,9)→古琴(guqin,1)"顺序摆放**(用户明确要求优先保证普通话，粤语其次因为用的人少)，最常用路径最先可用。新增访问器`getAudioData(track,id)`/`getGuqinClip()`/`audioClipReady(track,id)`(紧邻`AC`/`ambInit`)，改造了`reciteAudioReady`/`reciteHasAudio`/`reciteHasAudioYue`/`recitePlayAudio`的`_audioSrc`/`eventPlayAudio`/`eventNarrate`/`sleepTrack`(改返回track名字符串,新增`sleepTrackIds()`)/`sleepPlayCurrent`(取不到数据时静默跳下一首而非报错，限重试次数防死循环)/`ambPlayClip`等古琴5处引用等全部调用点。一次性迁移脚本`scripts/migrate_audio_to_lazy.js`(用`new Function`安全求值现有4个受控生成的JS字面量，逐字节diff校验194个数据块100%无损)转换了现有文件；转换前快照存档`archive/poemgraph_pre_lazy_audio_backup_20260706.html`；同步改造`scripts/gen_recite_audio.py`(新增`embed_ids`/`embed_clips`函数)，今后重新生成音频会直接输出新格式。**已验证**：转换后可执行JS部分从~12.2MB降到2.2KB(核心假设直接量化确认)；194个数据块逐字节校验与转换前完全一致；真实浏览器里`reciteHasAudio`/`reciteAudioReady`/`recitePlayAudio`/`eventNarrate`/`sleepTrack`/`sleepPlayCurrent`/`ambSetHifi`/`ambPlayClip`全部改造点真实调用验证正确(含边界：不存在的id正确返回null/false)；此前两轮修复(手机跟读rAF高频轮询、上一首/下一首按年级排序)在新数据层上回归测试通过(真实静音间隙检测结果与迁移前逐字节一致，如咏鹅仍是`[2.95,3.49,5.38,6.94]`)；`gen_recite_audio.py`新函数用mock HTML做了独立单元测试(增量更新已有诗+新增诗都正确，顺序不被打乱)；控制台0错误。**手机真机验证仍是最终判据**——本环境测不出真实"30秒→多少秒"的改善幅度，需要用户在同一台设备上分别测"微信内置浏览器打开本地文件"和"手机浏览器打开Render网址"，对比改造前后首次打开耗时。
- **安卓Chrome打开微信分享文件(content://)持续慢~20秒——诊断为安卓平台级瓶颈，非本项目代码可优化，仅记录不改代码**：上一轮"内嵌音频改惰性`<script type=text/plain>`"上线后，用户带来关键新证据：安卓真机(Redmi K40，骁龙870+12GB内存，**配置不弱**)用手机自带Chrome打开同一个`poemgraph.html`——若是通过微信分享收到、微信自己不支持预览该文件类型转而"用Chrome打开"(此时地址栏是`content://com.tencent...`)，**无论第1/2/3次都要约20秒**，没有"第二次变快"；但**同一台设备同一个Chrome**打开托管网址`https://pg-ndxn.onrender.com/`每次都只要1-2秒(含首次)。**诊断**：`content://`意味着Chrome是通过安卓Binder跨进程IPC向微信APP的`ContentProvider`讨要字节(历史上单次事务约1MB上限，12MB文件需多次跨进程往返)，这跟Chrome高度优化过的HTTP管线是完全不同的代码路径，且发生在**浏览器开始解析HTML之前**——页面自身任何JS/HTML优化(拆脚本、音频懒加载…)都够不着这一层，故上一轮修复对此场景"无效"是预期之内、不是没生效。且`content://`/`file://`协议下**无法注册Service Worker**(`poemgraph.html:4682/4690`的`location.protocol.indexOf('http')===0`门槛)，"利用本地缓存让第二次变快"这条路在"分享文件直接打开"场景下**从架构上就不存在**。**结论：非硬件问题、非本项目代码可修复的问题——是"微信分享文件→用Chrome打开"这条访问路径本身在安卓平台的性能天花板**。**手机端应主推托管网址作为主渠道**(已有SW缓存优先+ETag后台校验，首次访问后自动缓存、之后离线可用且快)，分享HTML文件直接打开只适合"完全没有网络"的最后兜底。用户已决定本轮不加"检测到非http(s)协议→建议改用网址"的提示条代码，只记录诊断。**诊断已获真机确认**：用户改用「QQ浏览器」打开同一份微信分享文件，1-2秒即开——关键证据是地址栏变成了真正的 `file:///storage/emul...`路径(不是`content://`)，说明QQ浏览器在接管分享文件时把内容落地成了真实本地文件再打开，绕开了Chrome那条"content://跨APP IPC读取"的慢路径；同一份文件、同样的12MB+体积、同样的手机，只因**接收方浏览器处理分享文件的方式不同**就有20秒 vs 1-2秒的天壤之别，完全印证了"瓶颈在content://访问机制本身，不在文件内容"这一诊断。**目前给用户的实用结论**：安卓上打开微信分享的这个HTML文件，选「QQ浏览器」（或其他能落地成`file://`真实路径的浏览器/查看器）比选「Chrome」明显更快；这是接收端APP的行为差异，不是本项目代码能左右的选择，长期仍建议以托管网址为主渠道。
- **朋友圈"影响"关系箭头不可见——已修复**：用户反馈欧阳修等人的"前辈提携"类关系线是虚线，但看不出箭头指向谁提携谁。**根因**：`renderCircle()`(`poemgraph.html:3009`)里箭头(`marker-end`/`marker-start`)的 DOM 属性其实一直是对的(用真实浏览器验证过 `<line>` 标签上确有 `marker-end="url(#relArrow)"`)，但线是画到对方圆心(节点圆 r=27，中心圆 r=37)，而节点圆是**后画**的实心图形，正好把落在线端点(=圆心)的箭头整个盖住——纯粹是层叠顺序问题，marker 定义/attrs 全程没错。**修复**：给带箭头的那一端算出退回到圆边缘外沿(节点端退 `R-29`，中心端退到 `39`)的坐标单独用于**可见的描边线**，不影响透明的大号命中区线(悬停/点击热区不变，仍用原始端点保证好点)和标签位置(仍用 `R*0.52`)。**验证**：真实浏览器里对比"缩短前后"两版渲染截图，缩短前 5 条边全部看不到箭头，缩短后欧阳修→苏轼/苏洵/苏辙/王安石/曾巩 5 个箭头清晰指向被提携者；反向验证苏轼视角下"陶渊明→苏轼""欧阳修→苏轼"两条虚线箭头正确指入中心节点；8+ 位诗人挨个 `openCircle` 无报错；控制台0错误。

## 部署/托管选型（2026-07-11 更正：双域名 + 双平台）
- **`poem.leewen.work` → Render Static Site**：`https://pg-ndxn.onrender.com/`（**主访问入口**，阿里 CNAME 绑上）。
- **`poem2.leewen.work` → 腾讯云 EdgeOne Pages**：`https://poemgraph.edgeone.dev/`（备用域名，2026-07-11 用户截图证实 EdgeOne 控制台可正常管理）。
- **撤销此前的"EdgeOne 大陆 401 硬拦"结论**：用户当时实测的 401 是另一个次级域名（不是这两个）；当前 EdgeOne 项目在 console.tencentcloud.com 上看是 healthy 状态。**教训**：跨平台测速/拦截不能用次级域名推断主域名的可用性——CNAME 后的具体 DNS 路由决定大陆能否访问。
- **仓库根 `index.html` 自动跳转到 `poemgraph.html`**，两个部署地址访问裸域名都直达应用。
- **Render 仍是主入口**：所有 poemgraph-pro.html 的迭代默认部署到 Render；admin 工具提示块 / 自动生成账号等 UI 也优先 Render。如未来想统一可逐步把 Render 退役。
- **腾讯云 EdgeOne 当前未跑业务**：只是"挂着"，作者已实名 + 项目已就绪。**未来若需大陆后端（云函数/CloudBase），可让 EdgeOne + 同账号 CloudBase 一起备案**——EdgeOne 的流量配额 + CloudBase 的云函数 + 数据库可一站式覆盖。

## 下一步 TODO（从这里继续）

- [ ] **2026-07-20 v6 bump 验证**：用户刷新后浏览器应拉新 HTML（DevTools → Application → Service Workers 确认 `poemgraph-cache-v6` 出现、旧 v5 已删；早发白帝城详情页 3 处文案应为"夜郎（今贵州北部、遵义附近）"）。
- [x] ~~`_headers` CORS 头为何未下发~~ → **误诊**：部署平台是 Render 不读 `_headers`，且同源优先策略已让 fetch 走通；`_headers` 文件留作将来迁回 Pages 时的兜底，对 Render 无害但也无用。
- [ ] **作者去 Render Dashboard 加 Headers（A 方案主路径，可选）**：打开 https://dashboard.render.com → `pg-ndxn` static site → 左侧菜单「Redirects/Headers」→ Add Header → Path: `/accounts.json` / Name: `Access-Control-Allow-Origin` / Value: `*` → 保存。**不强制**——B 方案同源优先已让 admin HTML 在 `pg-ndxn.onrender.com` / `poem.leewen.work` / 任意 origin 打开都能 fetch 同源的 `/accounts.json`。这条只在"以后想把 accounts.json 单独存到另一站（如备份 / R2）"才有意义，届时再加。
- [ ] **真机复测**：`https://pg-ndxn.onrender.com/account-admin.html` 打开 → console 0 CORS error + 「现有 accounts.json」textarea 自动填入 2 条 demo + 状态条绿色 ✓。
- [ ] **「🎲 生成账号」按钮 UX 提示**：按钮本身已工作，但用户点完看 JSON 输出框仍显 `empty` 会以为无响应——考虑在按钮邻近加一行小字「点了之后看上面『明文账号清单』，要点「🔗 追加到现有 JSON」或「🔄 同步到 JSON」才会出 JSON 输出框」。
- [ ] **详情页 panel 朗读·真机听感复测（仅剩）**：Playwright headless TTS 不真出声，**场景 1「原文朗读不乱读」听感**只能你真机确认一次。Playwright 3/5 场景已通过 + 守卫实测 + 0 console error，已排除一切可自动化验的问题。
- [x] UI/UX评审5项适龄修复 + 朗读跨平台稳健（`_ttsKeep`/cancel延时/recVoice重取）已提交 ✅
- [x] 历史大事讲故事"拖音"已修（清晰优先：`pickNarratorVoice` 只认清晰男声 Kangkang/云希，**排除 Apple 角色音 Reed/Eddy**，Apple 无则退清晰语舒）✅
- [x] 演示视频素材：`demo/showcase.html`（5张双语章节卡）+ `demo/VIDEO_SCRIPT.md`（90秒双语讲稿）+ `video-showcase.html`（正式录屏场景页，含旁白提示/镜头清单/真实应用跳转）已创建 ✅
- [x] **部署上线**：EdgeOne Pages(`https://peomgraph-dpw1a040zswi.edgeone.dev`，**大陆直连401被拦**，需VPN) + Cloudflare Pages(`https://pg-cyq.pages.dev`，未做大陆实测) 均已部署成功 ✅
- [ ] 待用户确认 Cloudflare Pages 在大陆直连(不用VPN)下的实际可用性——若也不行，则回退用 Render(用户另一项目实测过大陆无需VPN可访问)
- [ ] ICP备案暂缓：待真要做付费账号系统(CloudBase)时一次性办理，见上方"部署/托管选型"完整成本分析
- [ ] 诗词朗读音色：Apple 设备男声皆带拖音→目前用清晰女声(语舒)；如需男声需内嵌预渲染音频(评估过~1MB/9事件，体积可接受但未做)
- [ ] 双击展开为"封顶12克制版"，用户原想"纯自由展开(无上限)"——可改
- [ ] `related` 相关推荐字段多为空（autoQuiz/详情已健壮，可后补）
- [ ] **待用户真机验证**：手机跟读"咏鹅"等短停顿诗句的rAF高频轮询修复是否真的解决了"带出下一句半个字"问题（见上方"当前状态"对应条目）
- [x] 手机首次打开慢~30秒——音频惰性化对JS解析成本确实有效，但用户新证据显示"微信分享文件→Chrome打开(content://)"场景持续~20秒(非首次慢二次快)，已重新诊断为安卓content://跨APP读取的平台级瓶颈，非本项目代码可修复 ✅（诊断闭环，见上方"当前状态"对应条目）
- [x] content://诊断已获真机确认：改用QQ浏览器打开(地址栏落地为真实`file:///storage/...`路径)1-2秒即开，同一文件用Chrome(content://)持续~20秒——证实瓶颈在接收端浏览器处理分享文件的方式，非文件内容问题 ✅

## 文件地图
- `poemgraph.html` — 全部代码与数据（改动主要在这）；末尾第二个 `<script>` 块＝内嵌音频常量(11.6MB)，靠后台加载不阻塞交互
- `sw.js` — Service Worker 离线缓存层（仅 http(s) 托管时由 HTML 注册；缓存优先+ETag 后台校验+更新 toast）
- `docs/IMPL_NOTES.md` — **关键实现备忘**（改地图/图谱/朗读等前按需读）+ 素材授权署名义务
- `docs/PROJECT_PLAN.md` / `docs/DESIGN.md` — 里程碑进度/设计（按需）
- `CLAUDE.md` — 项目规则（视觉约束、文档维护、接手省 token 协议）
- `video-showcase.html` — 正式录屏展示页（场景化脚本+模拟截图框+真实应用跳转，不改 poemgraph.html 本体）
- `demo/showcase.html` + `demo/VIDEO_SCRIPT.md` — 演示视频章节卡+讲稿（不改 poemgraph.html 本体）
- 完整变更历史 → `git log`

## 变更记录

| 日期 | 变更内容 |
|------|---------|
| 2026-07-20 | 为李白 1–6 号诗人足迹补齐古今双行地名，并将《早发白帝城》白帝城／江陵标签改为错位引线布局；why：让编号点可读，并避免古今地名、圆点和专属航线互相重叠。 |
| 2026-07-20 | 为普通版与 Pro 增加学生角色专用的“我们”视角讲述转换，并同步详情朗读文本；why：避免直接对孩子说话时仍以“孩子”第三人称称呼学习者。 |
| 2026-07-20 | 为普通版与 Pro 的手机端顶部“年级”入口增加一致的 1–9 年级下拉菜单，并保留 Pro 权限锁定提示；why：修复在年级诗词列表页重复点击入口时没有任何视觉反馈的问题。 |
| 2026-07-20 | 在普通版与 Pro 诗词详情页的手机端右上角增加应用内返回图标按钮；why：让用户返回诗词列表时不必触发内置浏览器退出网页。 |
| 2026-07-20 | 将 Pro 付费横幅副文案调整为「¥价格 解锁全部内容 · 教师免费」；why：让价格先行、语序更自然。 |
| 2026-07-20 | 将 Pro 付费横幅桌面端的限制文案与解锁文案改为单行基线对齐，并保留小屏自动换行；why：减少横幅高度与空间浪费。 |
| 2026-07-20 | 删除 Pro 付费横幅的重复摘要，并将手机端改为简短朗读限制提示与直达共用付费框的解锁按钮；why：避免窄屏文案重复和不必要的展开层级。 |
| 2026-07-20 | 将手机微信端诗词解释朗读条改为仅在用户点击解释区小喇叭后显示，并清理 Pro 详情函数中的孤立回退代码；why：避免未操作时遮挡正文，同时恢复 Pro 页面脚本可执行性。 |
| 2026-07-20 | 修复 Pro 详情页段落朗读调用了闭包私有函数而中断的问题，并恢复底部操作栏播放状态同步；why：保证点击小喇叭即可朗读且不再抛 ReferenceError。 |
| 2026-07-20 | 将统编版三年级上册《早发白帝城》连同共情式诗人故事、地图/图谱/练习/炼字/相关诗词及普通话粤语离线朗读补入普通版和 Pro；why：修复教材诗词漏收并保持详情与朗读全链路完整。 |
| 2026-07-18 | 同步记录普通版与 Pro 版角色卡片图片替换及响应式回归结果，确保下一位 AI 可直接了解最新视觉状态。 |
